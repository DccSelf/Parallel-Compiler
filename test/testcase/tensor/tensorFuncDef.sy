void sub(tensor a, tensor b, tensor c){
    __sub(a, b, c);
    return;
}

int main(){
    tensor a[2][3] = {{0, 2, 3}, {4, 5, 6}};
    tensor b[2][3] = {{1, 2, 3}, {4, 5, 6}};
    tensor c[2][3];
    sub(a, b, c);
    puttensor(c);
    return 0;
}